# AGENTS.md

## 项目

拼豆图纸生成微信小程序：用户导入一张图片 →（可选 AI 卡通化）→ 像素化并映射到拼豆标准色 → 生成、展示、修改、导出拼豆图纸。

当前里程碑只包含三个页面：

1. 主页面：图片导入区域、拼豆色系选择（48/72/144/221 色）、拼豆盘大小选择、生成按钮
2. 图纸展示页：展示拼豆图纸，支持导出与进入修改
3. 图纸修改页：逐格改色；取色面板按“小盒子陈列”的方式展示当前色系的拼豆颜色

## 技术栈与目录

- 微信小程序 + 云开发（官方模板，已含分包结构与 weui）
- `miniprogram/` 为小程序代码根；新页面建议放在 `miniprogram/page/` 下，并在 `miniprogram/app.json` 的 `pages` 中注册
- `cloudfunctions/` 为云函数目录（图片处理、AI 卡通化等后端逻辑放这里）
- 色卡资料：`docs/拼豆标准色彩RGB与拼豆盘尺寸.md`；开发时应抽成独立 `colors.json` 静态数据供前端使用
- 官方模板自带的分包、tabBar、darkmode 等配置保持现状，除非任务明确要求修改

## 工作准则

### 1. 先思考再写码

- 明确假设再动手；不确定就问；存在多个合理解释时列出来，不要默默选一个
- 有更简单的做法就直接说，必要时提出反对意见

### 2. 简单优先

- 只写解决问题所需的最少代码；不做投机性抽象，不做未被要求的“灵活性/可配置性”
- 能压缩到更短就重写，避免过度设计

### 3. 外科手术式修改

- 只改必须改的地方，不顺手“优化”无关代码或格式
- 匹配现有代码风格（模板为 JS + WXML + WXSS，不使用 TS）
- 自己改出来的孤儿代码（未使用的 import/变量/函数）要清理；既有死代码只提不删
- 每一行改动都应能追溯到用户请求

### 4. 目标驱动执行

- 把任务转成可验证目标，例如“加校验”→“写无效输入用例并跑通”
- 多步任务先给简短计划：步骤 → 验证方式
- 小程序以“微信开发者工具编译通过、页面可交互”为验证标准；云函数改动需上传部署

## 数据与算法约定

- 默认色卡：MARD 221 色（`mard-221-alfonse-doudou`，见 docs 第 3 节）
- 48/72/144 色 = 221 色卡按套装色号取的子集（清单见 docs 第 4 节）
- 像素化后做最近色匹配（建议 CIELAB 或加权 RGB 距离），且只允许输出当前所选套装内的色号
- 拼豆盘（即拼豆板）大小以像素为单位：52×52 / 78×78 / 104×104 三档，作为图纸网格尺寸选项

## 验证

- 用微信开发者工具打开项目根目录，修改代码后点击编译
- 涉及 `miniprogram_npm` 的依赖改动后需执行“工具 → 构建 npm”
- 云函数改动需在开发者工具中右键“上传并部署（云端安装依赖）”
