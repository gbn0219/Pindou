# 开发进度

> 用于了解当前进度与最近改动。最新里程碑优先；历史记录按时间倒序。

## 当前状态

- 分支：`codex/pindou-pattern`（已推送到 `origin/codex/pindou-pattern`，仓库 `github.com/gbn0219/Pindou`）
- 微信小程序原生（JS + WXML + WXSS，无第三方 npm 依赖），共 **8 个页面**（index / profile / gallery / crop / pattern / pattern-edit / scan / feedback）
- 核心功能：照片还原 + 创意生成（豆包 Seedream）→ 展示/修改/导出/图库，以及**全屏拼豆**（智能拼豆 / 一键跟拼）

## 最近里程碑

### 2026-09-01 · 图纸识别重构：固定网格 + 移图 + 自动对齐 + 背景置空（commit `6a6b75a`）

**重构校准与背景（替代 08-31 初始版“拖框圈选 + 白格全算”）**
- 入口不变：首页 hero 下方「识别已有图纸」→ `page/scan/index`（第 8 页，不调用 AI）
- 交互：屏幕固定一张 R×C 网格（位置锁死），用户缩放/拖动**图片**对齐网格；改行/列数变网格密度；◀▶▲▼ 改为 1/10 格平移图片；去掉原“宽窄/高矮/重置”按钮，新增「自动对齐」「重置对齐」
- 自动对齐：`scan.detectGridLines` 投影峰值法检测格宽/格高/原点；初始整图铺进网格，识别成功自动对齐到图纸中间区域；失败/不准可手动缩放拖动
- 背景置空：`scan.sampleCell` 每格取主色 + 中心黑标占比（暗且低饱和，避开边缘网格线）；白格有黑标（打印色号，一般 H2）→ 白豆；无黑标 → 背景置空；整图无任何黑标 → 回退“全当白豆”；页面开关「背景自动识别/全当白豆」默认自动
- 纯函数：`sampleCell` / `scanScreenGrid` / `buildBgMask` / `detectGridLines` / `rgbGridToCodes`；`tests/scan.test.js` 覆盖黑标判定、无标号兜底、带/无外框网格检测
- 设计文档：`docs/superpowers/specs/2026-08-31-scan-pattern-design.md`
- 范围外（后续）：矩形图纸（展示/修改/导出需从单值 size 泛化到 rows/cols）、旋转/透视校正、扫描图入库图库

### 2026-08-31 · 界面统一与全屏拼豆优化（commit `20b2a7f`）

**界面一致性**
- 统一暖纸配色/卡片表面/选中态：黄=主按钮、墨=选中、红=强调/危险；补齐未定义 CSS 变量（`--color-card`、`--color-bead-hi`、`--color-cast-strong`、`--color-shadow-inset`）；清掉乱入的蓝色。

**图纸展示 / 修改**
- 画布显示区改 **1:1 正方形**，去掉四周坐标刻度条，居中画板；收紧画布卡内边距。
- 修复预览页**非全屏下双指缩放失效**（`onTouchMove` 非全屏分支为空，之前加"全屏"时漏掉）。

**全屏拼豆（pattern / pattern-edit 共用）**
- 去掉坐标条，网格铺满画布；底部面板占满剩余高度，色卡一行 **3 个、等宽、内容居左**；**多行色卡**铺满。
- 新增**网格选项**：网格线开关 + 密度（默认每 5 格，可切每 10 / 每 20）——目前只加在预览页全屏（修改页未渲染网格线）。
- **一键跟拼改为两步撤回**：点已拼完的颜色只是设为当前色、按钮变"撤回"；点"撤回"才真正取消；再点当前色取消选中。

**其他**
- 修复编辑页"放大键"点击无响应（去掉放大键上多余的 `catchtouchstart="noop"`）。
- `app.js` 加 `onPageNotFound` 路由兜底（空路由回首页，避免硬报错）。
- 新增 `utils/beading.js`、`styles/beading.wxss`、`tests/beading.test.js`。

**注意**：本轮的"大胆重设计（Bead Atelier 主页 + 配色 A/B）"已出方案/预览图，用户选择了 A 并短暂实现后**已撤回**，主页回到暖纸版。若后续要做，按 `docs/page-redesign-plan.md` 与 `miniprogram/styles/tokens.wxss` 为准。

## 待办 / 未做

- **裁剪页**：方案里提到"自由/1:1/3:4/4:3"比例分段控制，但当前裁剪是**任意比例方框**，未加该功能（用户确认不改）。
- **风格参考拼图**（`tools/style-refs/ref-pack.jpg`）已不再发送（Seedream 多图输入偶尔返回参考图本身），文件保留。
- 修改页的全屏拼豆**未渲染网格线**、未加网格选项（如需，要先给其 `draw` 加网格绘制 + `gridOn/gridEvery` 字段）。
- 图库 / 个人中心 / 反馈页在本轮主要是配色/布局一致性小改，结构未动。

## 验证

- 单测：`node tests/color.test.js && node tests/pattern.test.js && node tests/ai.test.js && node tests/user.test.js && node tests/progress.test.js && node tests/background.test.js && node tests/beading.test.js && node tests/gesture.test.js && node tests/scan.test.js`
- 新增/修改 JS：`node --check <file>`；JSON：`node -e "JSON.parse(...)"`
- 微信开发者工具：`D:\Tencent\Winxin_develop`（`cli.bat open --project D:\Code\Weixin\Pindou`），改动后点"编译"验证
- 逻辑约定：图纸数据 `globalData.pattern = { grid, size, set, imagePath, mode, style, bgMask }`；创意会话 `globalData.aiSession`；其余见根目录 `AGENTS.md`
